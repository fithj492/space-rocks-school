if (room== rm_game)
{
	spawn_off_camera(obj_asteroid, 6);
	alarm[0] = 60;
	score = 0;
}

if (room== rm_game)
{
	spawn_off_camera(obj_shotPowerUp,5);
	alarm[1] = 60;
}

if (room== rm_game)
{
	spawn_off_camera(obj_shipPowerUp, 2);
	alarm[1] = 60;
}



if (room== rm_game)
{
	spawn_off_camera(obj_sheild,2);
	alarm[2] = 60;
}

